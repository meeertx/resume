# dependencies
node_modules

# build output
dist
dist-ssr

# raw video sources (downloaded by scripts/fetch-assets.mjs, ~3.7MB each — not committed)
raw/

# env — real keys never committed; .env.example documents the shape
.env
.env.local
.env.*.local

# editor
*.local
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
Thumbs.db
.DS_Store
